extends layout

block content 
    h1 Really really loan book?    
    form(method='POST')
        p #{book_instance.book.title}
        //- p #[strong Due back: ] #{book_instance.due_back_formatted}
        button.btn.btn-primary(type='submit',id='confirmbtn') Confirm 
        hl &emsp;
        a(href='/catalog',style='display:inline-block;text-decoration:none')    
            button.btn.btn-primary(type='button',id='cancelbtn',style='background-color: #d30;.btn-primary:focus{visible:none;}') Cancel 


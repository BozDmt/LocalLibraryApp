extends layout 

block content 
    h1= title 

    if genre_list.length 
        ul.styled_list
            each genre in genre_list
                li 
                    a(href=genre.url) #{genre.name}
    else 
        p No available genres at the moment.